{
  "name": "postgresql-view",
  "version": "0.1.0",
  "private": true,
  "description": "Local web ERD viewer for PostgreSQL schemas",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js"
  }
}
